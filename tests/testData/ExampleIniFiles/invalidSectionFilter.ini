[DEFAULT]
prefixmap = prefix1:okPath prefix2:anotherPath

[invalidSectionFilterSection]
# This INI file is invalid because the filter is missing a closing bracket "]"
filter      =   [ .txt
respaths    =   prefix1:/firstLine/...
                prefix2:/secondLine/...
