[DEFAULT]
prefixmap = prefix1:okPath

[invalidInlineFilterSection]
filter      =   [ .txt ]
# This INI file is invalid because the inline filter is missing an opening bracket "["
respaths    =   prefix1:/firstLine/* .csv ]
                prefix2:/secondLine/...