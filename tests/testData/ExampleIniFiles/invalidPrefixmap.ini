[DEFAULT]
# This INI file is invalid because the prefixmap entry "prefix1:" has no actual paths defined
prefixmap = prefix1: prefix2:someValidPrefix

[invalidPrefixMapSection]
filter      =   [ .txt ]
respaths    =   prefix1:/firstLine/...
                prefix2:/secondLine/...
