[DEFAULT]
prefixmap = prefix1:okPath

[invalidPrefixmapMismatchSection]
filter      =   [ .txt ]
# This INI file is invalid because the prefixDoesNotExist prefix does not exist in the parent prefixmap
respaths    =   prefixDoesNotExist:/firstLine/*
                prefix2:/secondLine/...